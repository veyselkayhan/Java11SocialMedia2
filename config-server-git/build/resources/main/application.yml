server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/veyselkayhan/Java11ConfigServer.git
          default-label: main
          skip-ssl-validation: true
          username: veyselkayhan
          password: ${Java11ConfigServerAccessToken}